apply plugin: 'java'
apply plugin: 'maven-publish'
apply plugin: 'idea'

group = 'oliv.raspi.coffee'
version = '1.0'

repositories {
    mavenCentral()
    mavenLocal()
}

compileJava {
    options.compilerArgs << "-Xlint:deprecation"
}
